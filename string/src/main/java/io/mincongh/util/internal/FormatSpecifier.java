package io.mincongh.util.internal;

/**
 * Format specifier is used to format an argument to a target text. It indicates how an argument
 * should be processed and where they should be inserted in the text. The format specifier can be
 * used for general, character, numeric, date/time, percent or line separator arguments.
 *
 * @author Mincong Huang
 */
public class FormatSpecifier {}
